<select name="${escape(NAME)}" id="${escape(HTML_ID)}" class="${escape(CSS_CLASS)}" # IF C_DISABLED # disabled="disabled" # ENDIF # >
	# START options #
	# INCLUDE options.OPTION #
	# END options #
</select>
