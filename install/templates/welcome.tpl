<h1>{DISTRIBUTION}</h1>
{L_DISTRIBUTION_EXPLAIN}
<br />
{DISTRIBUTION_DESCRIPTION}

{i18n(lang.msg, array('couco' => toto), true)}

Comment trouver et capturer les déterminants le le les de des etc... suivis d'un mot (avec capture du mot) dans le texte suivant:

La synt{i18n(lang.msg, array('test' => my.value), true)}axe des
masques {i18n(lang.msg, array(), false)}dans ces
fon{i18n(lang.msg)}ctions ressemble fort à
celle de Perl. Les expressions seront
entourées de délimiteurs, slash (/), par exemple.
N'importe quel caractère peut servir de délimiteur,
tant qu'il n'est pas alpha-numérique ou n'est pas 
un anti-slash (\). Si un délimiteur doit être utilisé
 dans l'expression, il faudra l'échapper avec un 
 anti-slash. Depuis PHP 4.0.4, vous pouvez utiliser
  les délimiteurs (), {}, [], et <>, comme
   en Perl. Voir la syntaxe des masques pour 
plus d'explications. 